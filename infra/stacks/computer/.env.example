# Computer stack secrets — copy to .env (next to docker-compose.yml). NEVER commit .env.
# Only the on-demand `desktop` service needs a key. Claude drives that desktop, so it
# bills your Anthropic API account (console.anthropic.com → API keys), not a Claude plan.
ANTHROPIC_API_KEY=
